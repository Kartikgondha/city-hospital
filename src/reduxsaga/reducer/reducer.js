import * as ActionTypes from '../actiontype'

const initVal = {
    isLoding : false,
    user : null,
    error : ''
}

export const authreducer = (action, state=initVal)=>{
    switch(action.type){
        default :
        return state
    }
}